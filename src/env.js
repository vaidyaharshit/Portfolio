/* ==========================================================================
   CONTACT FORM SETUP — THE ONLY FILE YOU NEED TO EDIT
   ==========================================================================
   HOW TO GET YOUR EMAILJS KEYS (~2 minutes):

     1. Sign up / log in at  https://www.emailjs.com
     2. SERVICE ID   → Dashboard → Email Services  → your service → Service ID
     3. TEMPLATE ID  → Email Templates → your template → Template ID
     4. PUBLIC KEY   → Account → General → Public Key  (API Keys section)

   Paste each value between the quotes below, save, then refresh the page.

   Do NOT create extra window.__ENV__ blocks — there must be exactly ONE
   in this file, or values overwrite each other.

   Safe to publish: the public key can only SEND mail, never read your inbox.
   ========================================================================== */

window.__ENV__ = {

  /* ── EmailJS ─────────────────────────────────────────────────────────── */
  EMAILJS_SERVICE_ID:  '',   // e.g. 'service_abc123'
  EMAILJS_TEMPLATE_ID: '',   // e.g. 'template_xyz789'
  EMAILJS_PUBLIC_KEY:  '',   // e.g. 'user_XXXXXXXXXXXXXXX'

  /* ── GitHub (optional) ───────────────────────────────────────────────── */
  /* Personal Access Token for higher API rate limits (public repos only)  */
  GITHUB_TOKEN: '',

};
