/* ==========================================================================
   TEMPLATE — copy this file to src/env.js and fill in your values.
   ========================================================================== */

window.__ENV__ = {
  /* REQUIRED — get it free at https://web3forms.com (sent to your email).
     Contact form submissions arrive in that email account's inbox. */
  WEB3FORMS_ACCESS_KEY: 'YOUR_WEB3FORMS_ACCESS_KEY',

  /* OPTIONAL — set to 'true' to render an hCaptcha challenge. Also enable
     "Block Spam -> hCaptcha" in your web3forms.com dashboard. Empty = off. */
  WEB3FORMS_CAPTCHA: '',
};
